import { Box } from "@chakra-ui/layout"
import React from "react"
import SectionTitle from "../../shared/section_title"

const About = () => {
  return (
    <Box height="100vh" background="inherit">
      <SectionTitle  name=".aboutMe()" id="about"/>
    </Box>
  )
}

export default About
